name: docs_MolSelect
channels:

  - conda-forge

  - defaults
dependencies:
    # Base depends
  - python
  - pip
  - lark
  
  - pydata-sphinx-theme
  - sphinx-design
  - sphinx-copybutton
  - myst-parser




    # Pip-only installs
  - pip:
    - -e ../

